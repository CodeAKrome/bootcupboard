conda 23.11.0 requires archspec, which is not installed.
conda 23.11.0 requires boltons>=23.0.0, which is not installed.
conda 23.11.0 requires charset-normalizer, which is not installed.
conda 23.11.0 requires distro>=1.5.0, which is not installed.
conda 23.11.0 requires jsonpatch>=1.32, which is not installed.
conda 23.11.0 requires menuinst, which is not installed.
conda 23.11.0 requires platformdirs>=3.10.0, which is not installed.
conda 23.11.0 requires pluggy>=1.0.0, which is not installed.
conda 23.11.0 requires pycosat>=0.6.3, which is not installed.
conda 23.11.0 requires requests<3,>=2.28.0, which is not installed.
conda 23.11.0 requires ruamel-yaml<0.19,>=0.11.14, which is not installed.
conda 23.11.0 requires tqdm>=4, which is not installed.
conda 23.11.0 requires truststore>=0.8.0; python_version >= "3.10", which is not installed.
conda-index 0.3.0 requires click>=8, which is not installed.
conda-index 0.3.0 requires filelock, which is not installed.
conda-index 0.3.0 requires jinja2, which is not installed.
conda-index 0.3.0 requires more-itertools, which is not installed.
conda-index 0.3.0 requires ruamel.yaml, which is not installed.
conda-build 3.28.3 requires beautifulsoup4, which is not installed.
conda-build 3.28.3 requires chardet, which is not installed.
conda-build 3.28.3 requires filelock, which is not installed.
conda-build 3.28.3 requires jinja2, which is not installed.
conda-build 3.28.3 requires jsonschema>=4.19, which is not installed.
conda-build 3.28.3 requires libarchive-c, which is not installed.
conda-build 3.28.3 requires menuinst, which is not installed.
conda-build 3.28.3 requires pkginfo, which is not installed.
conda-build 3.28.3 requires psutil, which is not installed.
conda-build 3.28.3 requires pytz, which is not installed.
conda-build 3.28.3 requires pyyaml, which is not installed.
conda-build 3.28.3 requires requests, which is not installed.
conda-build 3.28.3 requires tqdm, which is not installed.
build 1.0.3 requires pyproject_hooks, which is not installed.
conda-libmamba-solver 23.12.0 requires boltons>=23.0.0, which is not installed.
conda-libmamba-solver 23.12.0 requires libmambapy>=1.5.3, which is not inst
